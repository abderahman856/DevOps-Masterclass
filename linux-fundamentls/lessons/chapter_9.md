**Shells (Bash vs Zsh)**

Check current shell:

    echo $SHELL

Switch shell:

    bash
    zsh

Change permanently:

**chsh -s /bin/zsh**

**Lesson 21 — Shell Configuration**

Edit:

    .bashrc
    .zshrc

Reload:

    source ~/.zshrc

Create alias:

    alias ll='ls -la'
